# Configuracoes do Banco PostgreSQL
# Le os dados de variaveis de ambiente da nuvem (Render, Railway, Heroku, etc.)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/devshowcase_prod}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Desativa o console H2 em producao por seguranca
spring.h2.console.enabled=false